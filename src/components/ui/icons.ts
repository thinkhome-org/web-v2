import * as TablerIcons from '@tabler/icons-react';

// Re-export Tabler icons under a single namespace to satisfy ESLint rule
// and avoid Turbopack issues with `export * as` re-exports.
export const Icons = TablerIcons;


